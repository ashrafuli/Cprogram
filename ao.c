#include<stdio.h>
int main()
{
  int a=6,b=2;
  int sum,div,mul,def;
  sum=a+b;
  printf("addition of two no:%d",sum);
  div=a/b;
  printf("division of two no:%d",div);
  mul=a*b;
  printf("addition of two no:%d",mul);
  def=a-b;
  printf("addition of two no:%d",def);
  return 0;
}
